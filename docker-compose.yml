services:
  db:
    image: mysql:8.0.0
    container_name: mysql
    environment:
      MYSQL_ROOT_PASSWORD: mysql
      MYSQL_DATABASE: personalization_db
    ports:
      - "3306:3306"
    volumes:
      - ./mysql:/var/lib/mysql